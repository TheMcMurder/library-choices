---
phase: 18-incorporate-feedback-from-library-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_data/config.js
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "Digital collections slider has 5 tiers ($5k/$15k/$30k/$55k/$65k) with $55k marked as current service level"
    - "Physical collections slider marks $15k as current service level, not $10k"
    - "Staffing card descriptions reflect librarian's real operational context (hours, staff coverage, accessibility impact)"
    - "Programming cost data is documented in config.js as a comment block for future reference"
    - "Source attributions on digital and physical collections reference NOTES.md, not old FY2025 budget proposal"
    - "draft: true remains unchanged"
  artifacts:
    - path: "src/_data/config.js"
      provides: "All site configuration data"
      contains: "value: 55000"
  key_links:
    - from: "src/_data/config.js"
      to: "test/config.test.js"
      via: "config structure validation"
      pattern: "pnpm test.*exits 0"
---

<objective>
Update config.js with real data from the librarian's notes (NOTES.md). Replace placeholder digital collection tiers with 5 real tiers, move the physical collections current-level marker, update staffing descriptions, add programming cost comment, and update source attributions.

Purpose: Incorporate real librarian feedback into the calculator so citizens see accurate service-level options and descriptions.
Output: Updated `src/_data/config.js` with real data; all existing tests still passing.
</objective>

<execution_context>
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-incorporate-feedback-from-library-notes/18-CONTEXT.md
@.planning/phases/18-incorporate-feedback-from-library-notes/NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config.js with real librarian data</name>
  <files>src/_data/config.js</files>
  <read_first>
    - src/_data/config.js (current state of config - understand structure, PLACEHOLDER markers, NON-DEVELOPER EDIT GUIDE)
    - .planning/phases/18-incorporate-feedback-from-library-notes/NOTES.md (librarian's raw data - source of truth for all values)
    - .planning/phases/18-incorporate-feedback-from-library-notes/18-CONTEXT.md (decisions D-01 through D-12)
  </read_first>
  <action>
Modify `src/_data/config.js` with the following changes. All changes are data-only inside the existing export default object. Do NOT change the NON-DEVELOPER EDIT GUIDE comment block at the top. Do NOT change property names, object structure, cities array, footerLinks, siteName, or draft flag.

**1. Replace collectionsDigital.options array (per D-01, D-02, D-03):**

Replace the entire `options` array inside `collectionsDigital` with these 5 entries (was 4):

```js
options: [
  { value: 5000,  isDefault: false, description: "Beehive only \u2014 substantially longer wait times for digital titles, shared with the entire state library system" },
  { value: 15000, isDefault: false, description: "Beehive + our own digital titles \u2014 longer wait times for most titles with a few select accelerated titles that we purchase" },
  { value: 30000, isDefault: false, description: "Beehive + expanded \u2014 longer wait times for most titles with some titles reduced via purchases" },
  { value: 55000, isDefault: true,  description: "Current service level (own Libby: $6k software + ~$28k new content + ~$20k repurchases annually)",
    isCurrentServiceLevel: true,
  },
  { value: 65000, isDefault: false, description: "Beehive + expanded \u2014 shorter wait times for most popular books" },
],
```

Also update `collectionsDigital.source` from `"Cache County Library Services budget proposal FY2025"` to `"Librarian cost data (NOTES.md)"`.

**2. Update collectionsPhysical (per D-04, D-05, D-06, D-07):**

Move `isCurrentServiceLevel: true` from the $10,000 option to the $15,000 option. Move `isDefault: true` from the $15,000 option to the $15,000 option (it stays on $15k, which is correct). The resulting options array:

```js
options: [
  { value: 0,     isDefault: false, description: "No physical print collection \u2014 digital only" },
  { value: 5000,  isDefault: false, description: "Small rotating print collection" },
  { value: 10000, isDefault: false, description: "Reduced core collection \u2014 books and DVDs, limited specialty items" },
  { value: 15000, isDefault: true,  description: "Current collection \u2014 books, DVDs, exploration kits, and equipment (telescopes, audiobook players)",
    isCurrentServiceLevel: true,
  },
  { value: 20000, isDefault: false, description: "Full print + AV materials + periodicals" },
],
```

Also update `collectionsPhysical.source` from `"Cache County Library Services budget proposal FY2025"` to `"Librarian cost data (NOTES.md)"`.

**3. Update staffingLevels descriptions (per D-08, D-09, D-10):**

Update the `description` field on each staffing level. Do NOT change id, label, annualCost, schedule, source, or isCurrentServiceLevel on any staffing level. Keep `source: "Cache County HR salary schedule FY2025"` on all three (per D-10).

- "1fte" (Basic): `"One full-time staff member covering all open hours. Reduced hours limit accessibility \u2014 no evening or weekend access for working citizens."`
- "1fte-1pte" (Extended): `"One full-time and one part-time staff member. Some hours outside 9\u20135 for working citizens, but limited evening and no weekend coverage."`
- "1fte-2pte" (Current): `"Two staff working at a time Mon\u2013Fri, open 44 hrs/week with 10 hours outside normal 9\u20135. Enables working citizens to access the library evenings and Saturdays."`

**4. Add programming cost comment block (per D-11):**

Add the following comment block immediately BEFORE the `cities` array (after the closing `},` of `collectionsPhysical`):

```js
/*
 * Programming costs (from librarian notes — not yet modeled as a slider):
 *   Summer reading: ~$1,000-$1,500/year
 *   Storytime: ~$300/year
 *   Other programs: ~$1,000-$2,000/year
 *   Total estimate: ~$2,300-$4,800/year
 */
```

**5. Confirm draft: true unchanged (per D-12):**

Do NOT modify `draft: true`. It must remain as-is.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - config.js collectionsDigital.options has exactly 5 entries (grep for `value: 5000` through `value: 65000` in that section)
    - config.js contains `value: 55000` with `isCurrentServiceLevel: true` and `isDefault: true` on the same option object
    - config.js collectionsPhysical options: `isCurrentServiceLevel: true` appears on the $15,000 option, NOT on the $10,000 option
    - config.js collectionsPhysical $10,000 description contains "Reduced core collection"
    - config.js collectionsPhysical $15,000 description contains "exploration kits"
    - config.js staffingLevels[0] description contains "no evening or weekend access"
    - config.js staffingLevels[1] description contains "limited evening"
    - config.js staffingLevels[2] description contains "44 hrs/week"
    - config.js contains comment block with "Programming costs" and "$2,300-$4,800/year"
    - config.js contains `draft: true`
    - config.js collectionsDigital.source contains "NOTES.md"
    - config.js collectionsPhysical.source contains "NOTES.md"
    - config.js staffingLevels source fields still contain "Cache County HR salary schedule FY2025"
    - `pnpm test` exits 0 with all tests passing
    - `pnpm run build` exits 0
  </acceptance_criteria>
  <done>config.js updated with all 5 digital tiers, corrected physical current-level marker, real staffing descriptions, programming cost comment, and updated source attributions. All tests and build pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify URL encoding handles new 5th digital tier</name>
  <files>test/config.test.js</files>
  <read_first>
    - test/config.test.js (current config structure tests)
    - src/js/lib/url-helpers.js (encodeIndices/decodeIndices — confirm bounds checking handles index 4)
    - src/_data/config.js (updated config from Task 1)
  </read_first>
  <action>
Read `test/config.test.js` and `src/js/lib/url-helpers.js` to confirm that the existing bounds-checking logic in `decodeIndices` handles index 4 (the new 5th digital tier) without any code changes.

Then add one targeted test to `test/config.test.js` inside the existing `collectionsDigital` describe block to assert the new data shape:

```js
it('has exactly 5 options with values 5000, 15000, 30000, 55000, 65000', () => {
  const values = config.collectionsDigital.options.map(o => o.value);
  expect(values).toEqual([5000, 15000, 30000, 55000, 65000]);
});
```

Also add a test in the `collectionsPhysical` describe block to lock in the current-level marker position:

```js
it('marks $15,000 as current service level', () => {
  const current = config.collectionsPhysical.options.find(o => o.isCurrentServiceLevel === true);
  expect(current.value).toBe(15000);
});
```

And add a matching test in the `collectionsDigital` describe block:

```js
it('marks $55,000 as current service level', () => {
  const current = config.collectionsDigital.options.find(o => o.isCurrentServiceLevel === true);
  expect(current.value).toBe(55000);
});
```

Run `pnpm test` to confirm all tests pass including the new ones.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - test/config.test.js contains `expect(values).toEqual([5000, 15000, 30000, 55000, 65000])`
    - test/config.test.js contains `expect(current.value).toBe(15000)` in collectionsPhysical block
    - test/config.test.js contains `expect(current.value).toBe(55000)` in collectionsDigital block
    - `pnpm test` exits 0 with all tests passing (including 3 new assertions)
  </acceptance_criteria>
  <done>Config structure tests lock in the new digital tier values and both current-service-level marker positions. All tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm test` exits 0 — all existing and new config structure tests pass
2. `pnpm run build` exits 0 — 11ty site builds successfully with updated config
3. `grep -c "isCurrentServiceLevel: true" src/_data/config.js` returns exactly 3 (one per: staffing, digital, physical)
4. `grep "value: 55000" src/_data/config.js` returns a match (new digital tier exists)
5. `grep "draft: true" src/_data/config.js` returns a match (watermark preserved)
6. `grep "Programming costs" src/_data/config.js` returns a match (comment block present)
</verification>

<success_criteria>
- Digital collections slider has 5 tiers with correct values and $55k marked current
- Physical collections current-level marker is on $15k, not $10k
- Staffing descriptions use librarian's real operational language
- Programming costs documented as comment for future reference
- Source attributions reference NOTES.md on both collection sliders
- draft: true unchanged
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/18-incorporate-feedback-from-library-notes/18-01-SUMMARY.md`
</output>
