---
phase: 02-data-and-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/index.html
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - TRST-01
  - TRST-02

must_haves:
  truths:
    - "Staffing radio buttons render from config.staffingLevels with cost and description visible"
    - "Collections checkbox renders from config.collections with cost and description visible"
    - "City checkboxes render from config.cities with household counts visible"
    - "Source citations appear for staffing costs, collections budget, and household counts"
    - "Explanatory copy is present alongside each staffing level and the collections toggle"
    - "No cost value, label, or city name is hardcoded in the template — all come from config"
    - "window.LIBRARY_DATA contains the full config object for Phase 3"
  artifacts:
    - path: "src/index.html"
      provides: "Complete form with data-driven controls, citations, descriptions, and LIBRARY_DATA embedding"
      min_lines: 80
  key_links:
    - from: "src/index.html"
      to: "src/_data/config.js"
      via: "Nunjucks for loops over config.staffingLevels, config.cities"
      pattern: "for level in config.staffingLevels"
    - from: "src/index.html"
      to: "window.LIBRARY_DATA"
      via: "dump | safe filter in script tag"
      pattern: "config \\| dump \\| safe"
    - from: "src/index.html staffing radios"
      to: "Phase 3 calculator"
      via: "data-cost attribute on radio inputs"
      pattern: "data-cost"
    - from: "src/index.html city checkboxes"
      to: "Phase 3 calculator"
      via: "data-households attribute on checkbox inputs"
      pattern: "data-households"
---

<objective>
Replace the pipeline-verification index.html with a complete data-driven form rendering all controls from config.js.

Purpose: Deliver the visible UI for Phase 2 — staffing radio buttons, collections toggle, city checkboxes — all rendered via Nunjucks for-loops from config data. Include source citations, explanatory copy, and the window.LIBRARY_DATA embedding for Phase 3. Apply the UI-SPEC design contract (Tailwind classes, spacing, typography, colors).

Output: Complete index.html with all form controls, styled per UI-SPEC, with no hardcoded values.
</objective>

<execution_context>
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-and-controls/02-CONTEXT.md
@.planning/phases/02-data-and-controls/02-RESEARCH.md
@.planning/phases/02-data-and-controls/02-UI-SPEC.md
@.planning/phases/02-data-and-controls/02-01-SUMMARY.md

<interfaces>
<!-- config.js schema created by Plan 01 — these are the data structures the template iterates over -->

From src/_data/config.js (after Plan 01):
```javascript
export default {
  siteName: "Cache County Library Service Choices",
  staffingLevels: [
    { id: "1fte", label: "1 Full-Time Librarian", annualCost: 150000, description: "...", source: "..." },
    { id: "1fte-1pte", label: "1 Full-Time + 1 Part-Time", annualCost: 190000, description: "...", source: "..." },
    { id: "1fte-2pte", label: "1 Full-Time + 2 Part-Time", annualCost: 230000, description: "...", source: "..." }
  ],
  collections: {
    annualCost: 30000,
    description: "New books, periodicals, and digital media added to the collection each year.",
    source: "Cache County Library Services budget proposal FY2025"
  },
  cities: [
    { id: "providence", label: "Providence", households: 2100, source: "Cache County Assessor 2024" },
    { id: "nibley", label: "Nibley", households: 1800, source: "Cache County Assessor 2024" },
    { id: "millville", label: "Millville", households: 950, source: "Cache County Assessor 2024" },
    { id: "river-heights", label: "River Heights", households: 620, source: "Cache County Assessor 2024" }
  ]
};
```

From eleventy.config.js (after Plan 01):
```javascript
// toLocaleString filter available — formats 150000 as "150,000"
eleventyConfig.addFilter("toLocaleString", (num) => Number(num).toLocaleString("en-US"));
```

Tailwind classes from UI-SPEC component inventory:
```
Page wrapper: <main> with max-w-2xl mx-auto px-8 py-12
Section heading <h1>: text-2xl font-semibold text-gray-900 mb-2
Fieldset: bg-gray-50 border border-gray-200 rounded-lg p-6 space-y-4
Legend: text-xl font-semibold text-gray-900 px-1
Radio/checkbox wrapper <div>: flex flex-col gap-2
Input (radio/checkbox): accent-blue-600
Control label: text-base font-semibold text-gray-900 cursor-pointer
Description text <p>: text-sm text-gray-600 leading-normal
Citation <cite>: text-sm text-gray-500 not-italic
Households count <span>: text-sm text-gray-500
Result placeholder <div id="result">: mt-8 p-6 bg-gray-50 border border-gray-200 rounded-lg
Fieldset spacing between sections: space-y-8 on the form/container
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace index.html with data-driven form controls</name>
  <files>src/index.html</files>
  <read_first>
    - src/index.html (current pipeline-verification placeholder — will be replaced entirely)
    - src/_data/config.js (Plan 01 output — the data schema that drives all template loops)
    - .planning/phases/02-data-and-controls/02-UI-SPEC.md (complete visual contract: Tailwind classes, layout, typography, colors, copywriting)
    - .planning/phases/02-data-and-controls/02-RESEARCH.md (Nunjucks for-loop patterns, dump|safe pattern, data attribute contract)
    - .planning/phases/02-data-and-controls/02-CONTEXT.md (locked decisions on cost display, citation format)
  </read_first>
  <action>
Replace the entire contents of `src/index.html` with a complete Nunjucks template that renders all form controls from `config` data. No value may be hardcoded in the template — every label, cost, description, citation, and household count comes from `config`.

**Complete template structure (write this exactly, adjusting only whitespace/formatting):**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ config.siteName }}</title>
    <link rel="stylesheet" href="/css/compiled.css" />
  </head>
  <body class="bg-white text-gray-900">
    <main class="max-w-2xl mx-auto px-8 py-12">

      <h1 class="text-2xl font-semibold text-gray-900 mb-2">{{ config.siteName }}</h1>

      <form id="configurator" class="space-y-8">

        {# --- Staffing Level --- #}
        <fieldset class="bg-gray-50 border border-gray-200 rounded-lg p-6 space-y-4">
          <legend class="text-xl font-semibold text-gray-900 px-1">Staffing Level</legend>
          {% for level in config.staffingLevels %}
            <div class="flex flex-col gap-2">
              <div class="flex items-baseline gap-2">
                <input
                  type="radio"
                  name="staffing"
                  id="staffing-{{ level.id }}"
                  value="{{ level.id }}"
                  data-cost="{{ level.annualCost }}"
                  class="accent-blue-600"
                  {% if loop.first %}checked{% endif %}
                />
                <label for="staffing-{{ level.id }}" class="text-base font-semibold text-gray-900 cursor-pointer">
                  {{ level.label }} &mdash; ${{ level.annualCost | toLocaleString }}/year
                </label>
              </div>
              <p class="text-sm text-gray-600 leading-normal">{{ level.description }}</p>
              <cite class="text-sm text-gray-500 not-italic">Source: {{ level.source }}</cite>
            </div>
          {% endfor %}
        </fieldset>

        {# --- Collections Budget --- #}
        <fieldset class="bg-gray-50 border border-gray-200 rounded-lg p-6 space-y-4">
          <legend class="text-xl font-semibold text-gray-900 px-1">Collections Budget</legend>
          <div class="flex flex-col gap-2">
            <div class="flex items-baseline gap-2">
              <input
                type="checkbox"
                id="collections"
                name="collections"
                data-cost="{{ config.collections.annualCost }}"
                class="accent-blue-600"
                checked
              />
              <label for="collections" class="text-base font-semibold text-gray-900 cursor-pointer">
                Include collections (+${{ config.collections.annualCost | toLocaleString }}/year)
              </label>
            </div>
            <p class="text-sm text-gray-600 leading-normal">{{ config.collections.description }}</p>
            <cite class="text-sm text-gray-500 not-italic">Source: {{ config.collections.source }}</cite>
          </div>
        </fieldset>

        {# --- Participating Cities --- #}
        <fieldset class="bg-gray-50 border border-gray-200 rounded-lg p-6 space-y-4">
          <legend class="text-xl font-semibold text-gray-900 px-1">Participating Cities</legend>
          {% for city in config.cities %}
            <div class="flex flex-col gap-2">
              <div class="flex items-baseline gap-2">
                <input
                  type="checkbox"
                  name="cities"
                  id="city-{{ city.id }}"
                  value="{{ city.id }}"
                  data-households="{{ city.households }}"
                  class="accent-blue-600"
                  checked
                />
                <label for="city-{{ city.id }}" class="text-base font-semibold text-gray-900 cursor-pointer">
                  {{ city.label }} <span class="text-sm text-gray-500">({{ city.households | toLocaleString }} households)</span>
                </label>
              </div>
              <cite class="text-sm text-gray-500 not-italic">Source: {{ city.source }}</cite>
            </div>
          {% endfor %}
        </fieldset>

      </form>

      {# --- Result Placeholder --- #}
      <div id="result" class="mt-8 p-6 bg-gray-50 border border-gray-200 rounded-lg">
        <h2 class="text-xl font-semibold text-gray-900">Annual cost per household</h2>
        <p class="text-sm text-gray-600 mt-2">Select options above to see the estimated cost.</p>
      </div>

    </main>

    <script>
      window.LIBRARY_DATA = {{ config | dump | safe }};
    </script>
    <script src="/js/calculator.js"></script>
  </body>
</html>
```

**Critical details:**

1. **Staffing radios:** Each has `data-cost="{{ level.annualCost }}"` for Phase 3. First radio is pre-checked via `{% if loop.first %}checked{% endif %}`. Cost shown inline in label as `${{ level.annualCost | toLocaleString }}/year` per CONTEXT.md recommendation.

2. **Collections checkbox:** Has `data-cost="{{ config.collections.annualCost }}"` for Phase 3. Checked by default. Cost shown in label text.

3. **City checkboxes:** Each has `data-households="{{ city.households }}"` for Phase 3. All checked by default. Household count shown inline in label with `toLocaleString` formatting.

4. **Citations:** Every control section has a `<cite>` element rendering `Source: {{ X.source }}`. Plain text, no links (per CONTEXT.md).

5. **Descriptions:** Staffing levels each have a `<p>` with `{{ level.description }}`. Collections has `<p>` with `{{ config.collections.description }}`.

6. **window.LIBRARY_DATA:** Embedded at end of body via `{{ config | dump | safe }}`. Must use `| safe` to prevent HTML entity encoding inside script tag.

7. **calculator.js script tag:** Referenced at end of body. Currently a placeholder from Plan 01.

8. **No hardcoded values:** Template has zero literal cost numbers, zero literal city names, zero literal descriptions. Everything comes from `config.*`.

9. **h1 uses font-semibold** (not font-bold) per UI-SPEC correction from Phase 1 scaffold.

10. **Body class:** `bg-white text-gray-900` only (remove the `p-8` from Phase 1 — page padding is now on `<main>` via `px-8 py-12`).

11. **CSS link:** Keep `href="/css/compiled.css"` — HtmlBasePlugin handles pathPrefix rewriting.

12. **Form has no action or submit button** — it exists as semantic grouping only in Phase 2.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && pnpm run build 2>&1 && echo "--- Radio count ---" && grep -c 'type="radio"' _site/index.html && echo "--- Checkbox count ---" && grep -c 'type="checkbox"' _site/index.html && echo "--- data-cost count ---" && grep -c 'data-cost=' _site/index.html && echo "--- data-households count ---" && grep -c 'data-households=' _site/index.html && echo "--- cite count ---" && grep -c '<cite' _site/index.html && echo "--- LIBRARY_DATA ---" && grep -c 'LIBRARY_DATA' _site/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build` exits 0
    - `_site/index.html` contains exactly 3 `type="radio"` inputs (one per staffing level)
    - `_site/index.html` contains exactly 5 `type="checkbox"` inputs (1 collections + 4 cities)
    - `_site/index.html` contains exactly 4 `data-cost=` attributes (3 staffing radios + 1 collections checkbox)
    - `_site/index.html` contains exactly 4 `data-households=` attributes (one per city)
    - `_site/index.html` contains at least 8 `<cite` elements (3 staffing + 1 collections + 4 cities)
    - `_site/index.html` contains `window.LIBRARY_DATA` with valid JSON (no `&quot;` escaping)
    - `_site/index.html` contains `font-semibold` on the h1 (not `font-bold`)
    - `_site/index.html` does NOT contain the Phase 1 placeholder text "Pipeline verification"
    - `_site/index.html` contains `150,000` (toLocaleString filter working on staffing cost)
    - `_site/index.html` contains `2,100` (toLocaleString filter working on household count)
    - No literal cost numbers appear in `src/index.html` — only Nunjucks expressions (grep for /\$[0-9]/ in src/index.html should return 0 matches)
    - `_site/index.html` contains `<form id="configurator"`
    - `_site/index.html` contains `<div id="result"`
    - `_site/index.html` contains `calculator.js` script reference
  </acceptance_criteria>
  <done>index.html renders all form controls from config data with correct Tailwind styling, citations, descriptions, data attributes for Phase 3, and LIBRARY_DATA embedding. Build passes. No hardcoded values in template.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify rendered page in browser</name>
  <files>src/index.html</files>
  <action>
    Human visual verification of the rendered page. No code changes — this task confirms Task 1 output looks correct in a browser.
  </action>
  <what-built>
    Complete data-driven form with:
    - 3 staffing level radio buttons showing cost and description
    - 1 collections budget checkbox with cost and description
    - 4 city checkboxes with household counts
    - Source citations under every control
    - Result placeholder section
    - All values pulled from config.js (no hardcoded text)
  </what-built>
  <how-to-verify>
    1. Run `pnpm run dev` (or `pnpm run build && pnpm run serve` if no dev script)
    2. Open the local URL in a browser
    3. Verify:
       - Page heading shows "Cache County Library Service Choices"
       - Staffing Level section has 3 radio buttons with costs ($150,000, $190,000, $230,000) and descriptions
       - Collections Budget section has 1 checkbox with "$30,000/year" and description
       - Participating Cities section has 4 checkboxes (Providence, Nibley, Millville, River Heights) with household counts
       - Every section has a "Source:" citation line
       - Result placeholder shows "Annual cost per household" heading
       - First staffing radio is pre-selected, collections and all cities are checked
       - Layout is centered, readable, with gray fieldset backgrounds
    4. Open browser DevTools Console and type `window.LIBRARY_DATA` — verify it contains the full config object with staffingLevels, collections, and cities
  </how-to-verify>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && pnpm run build && echo "Build OK — open in browser to visually verify"</automated>
  </verify>
  <done>Human confirms the rendered page displays all controls correctly, citations are visible, layout looks clean, and window.LIBRARY_DATA is populated in the console.</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `pnpm run build` exits 0
2. `grep -c 'type="radio"' _site/index.html` returns 3
3. `grep -c 'type="checkbox"' _site/index.html` returns 5
4. `grep -c 'data-cost=' _site/index.html` returns 4
5. `grep -c 'data-households=' _site/index.html` returns 4
6. `grep -c '<cite' _site/index.html` returns at least 8
7. `grep 'LIBRARY_DATA' _site/index.html` returns a match with valid JSON
8. `grep -c 'PLACEHOLDER' src/_data/config.js` returns 8 (from Plan 01)
9. No hardcoded cost/city values in src/index.html template source
</verification>

<success_criteria>
- All form controls render from config data with no hardcoded values
- Staffing radio buttons show cost inline, descriptions below, citations below
- Collections toggle shows cost, description, citation
- City checkboxes show household counts, citations
- data-cost and data-households attributes present for Phase 3
- window.LIBRARY_DATA embedded with full config
- UI matches UI-SPEC design contract (spacing, typography, colors)
- Human verification confirms visual correctness in browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-and-controls/02-02-SUMMARY.md`
</output>
