---
phase: 07-collections-budget-slider
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - src/js/url.js
autonomous: true
gap_closure: true
requirements:
  - SLDR-05
  - SLDR-08

must_haves:
  truths:
    - "Opening ?collections=40000 in a new tab shows the $40k description, $40,000 amount, and correct aria-valuetext — not the $30k defaults"
    - "Opening ?collections=10000 shows Digital-only access description and $10,000 amount immediately without user interaction"
  artifacts:
    - path: "src/js/url.js"
      provides: "Input event dispatch after URL restoration"
      contains: "new Event('input')"
  key_links:
    - from: "src/js/url.js"
      to: "src/js/calculator.js"
      via: "form.dispatchEvent(new Event('input')) triggers updateSliderLabels()"
      pattern: "dispatchEvent.*new Event.*input"
---

<objective>
Fix URL restoration so that opening a shared URL with a non-default collections value (e.g. ?collections=40000) updates the slider description, dollar amount display, and aria-valuetext — not just the slider position and tax result.

Purpose: Completes SLDR-05 (aria-valuetext after URL restore) and SLDR-08 (full UI sync after URL restore), which were partially satisfied by plan 07-01.

Output: One-line addition to src/js/url.js.
</objective>

<execution_context>
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-collections-budget-slider/07-01-VERIFICATION.md
@.planning/phases/07-collections-budget-slider/07-01-SUMMARY.md

<interfaces>
<!-- Current event wiring in calculator.js (already implemented by 07-01) -->

From src/js/calculator.js (event listeners, lines 78-89):
```javascript
form.addEventListener('change', updateResult);
form.addEventListener('input', function () {
  updateResult();
  updateSliderLabels();
});
// ...
updateResult();
updateSliderLabels();
```

Key fact: `updateSliderLabels()` is ONLY called via the `input` event listener and the initial page-load call. The `change` event listener only calls `updateResult()`.

From src/js/url.js (lines 81-83, the code to fix):
```javascript
restoreFromUrl();
form.dispatchEvent(new Event('change'));
form.addEventListener('change', encodeUrl);
```

The `change` dispatch triggers `updateResult()` but NOT `updateSliderLabels()`. Adding an `input` dispatch will trigger the `input` listener which calls both `updateResult()` and `updateSliderLabels()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch input event after URL restoration in url.js</name>
  <files>src/js/url.js</files>
  <read_first>
    - src/js/url.js (full file — 84 lines; line 82 is the change dispatch to fix)
    - src/js/calculator.js (confirm input listener calls updateSliderLabels — lines 81-84)
  </read_first>
  <action>
In `src/js/url.js`, add one line after line 82 (`form.dispatchEvent(new Event('change'));`):

```javascript
form.dispatchEvent(new Event('input'));
```

The result at lines 81-84 should be:

```javascript
restoreFromUrl();
form.dispatchEvent(new Event('change'));
form.dispatchEvent(new Event('input'));
form.addEventListener('change', encodeUrl);
```

This causes the `input` event listener in `calculator.js` to fire after URL restoration, which calls both `updateResult()` and `updateSliderLabels()`. The `updateSliderLabels()` call reads the restored slider value and updates:
- `#collections-amount` textContent (e.g. "$40,000")
- `#collections-description` textContent (e.g. "Expanded print + digital + periodicals")
- `aria-valuetext` attribute (e.g. "40,000 dollars — Expanded print + digital + periodicals")

The existing `change` dispatch on line 82 stays — it triggers `updateResult()` for the tax calculation and is harmless (the `input` listener also calls `updateResult()`, but calling it twice on page load is negligible).

Do NOT modify any other file. Do NOT change any other line in url.js.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/js/url.js line 83 contains exactly `form.dispatchEvent(new Event('input'));`
    - src/js/url.js contains both `new Event('change')` and `new Event('input')` dispatches in sequence
    - src/js/url.js still contains `form.addEventListener('change', encodeUrl);` after both dispatches
    - src/js/url.js total line count is 85 (was 84, added 1 line)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>url.js dispatches both change and input events after restoreFromUrl(). Opening ?collections=40000 in a new tab now triggers updateSliderLabels(), fully synchronizing #collections-description, #collections-amount, and aria-valuetext with the restored slider value.</done>
</task>

</tasks>

<verification>
After task completes:

1. `pnpm build` exits 0
2. `grep -n "new Event('input')" src/js/url.js` shows the new dispatch line
3. `grep -c "dispatchEvent" src/js/url.js` returns 2 (one change, one input)
4. Open `_site/index.html?collections=40000` — description shows "Expanded print + digital + periodicals", amount shows "$40,000", aria-valuetext includes "40,000 dollars"
5. Open `_site/index.html?collections=10000` — description shows "Digital-only access via Beehive and Libby", amount shows "$10,000"
6. Open `_site/index.html` (no params) — default $30k description still works as before
</verification>

<success_criteria>
- url.js dispatches `new Event('input')` after `new Event('change')` following restoreFromUrl()
- No other files modified
- pnpm build succeeds
- URL restoration fully synchronizes all slider UI elements (description, amount, aria-valuetext)
</success_criteria>

<output>
After completion, create `.planning/phases/07-collections-budget-slider/07-02-SUMMARY.md`
</output>
