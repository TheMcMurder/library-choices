---
phase: 22-usage-and-other-analytics-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/js/lib/analytics.js
  - test/analytics.test.js
  - src/_data/config.js
  - src/index.html
autonomous: true
requirements: [ANA-01, ANA-02, ANA-03, ANA-04, ANA-05]

must_haves:
  truths:
    - "GA4 snippet appears in built HTML when ga4MeasurementId is set"
    - "GA4 snippet is absent from built HTML when ga4MeasurementId is null"
    - "analytics.js wrapper no-ops silently when window.gtag is undefined"
    - "analytics.js wrapper calls window.gtag with correct event names and params when gtag exists"
    - "GA4 does not initialize on localhost"
  artifacts:
    - path: "src/js/lib/analytics.js"
      provides: "Named wrapper functions for each GA4 custom event"
      exports: ["trackStaffingSelected", "trackDigitalSliderChanged", "trackPhysicalSliderChanged", "trackCityToggled", "trackBreakdownOpened", "trackSharedUrlLoaded"]
    - path: "test/analytics.test.js"
      provides: "Unit tests for analytics wrapper guard and event parameters"
    - path: "src/_data/config.js"
      provides: "ga4MeasurementId field (null placeholder)"
    - path: "src/index.html"
      provides: "Conditional GA4 script snippet in head"
  key_links:
    - from: "src/index.html"
      to: "src/_data/config.js"
      via: "Nunjucks {% if config.ga4MeasurementId %}"
      pattern: "config\\.ga4MeasurementId"
    - from: "src/js/lib/analytics.js"
      to: "window.gtag"
      via: "typeof window.gtag === 'function' guard"
      pattern: "typeof window\\.gtag"
---

<objective>
Create the GA4 analytics foundation: the analytics.js wrapper module with unit tests, the ga4MeasurementId config field, and the conditional GA4 snippet in the HTML template.

Purpose: Establishes all analytics infrastructure so Plan 02 can wire event calls into existing JS files.
Output: analytics.js module (tested), config.js updated, index.html updated with GA4 snippet.
</objective>

<execution_context>
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-usage-and-other-analytics-tracking/22-CONTEXT.md
@.planning/phases/22-usage-and-other-analytics-tracking/22-RESEARCH.md

<interfaces>
<!-- Existing lib helper pattern to follow -->
From src/js/lib/calculator-helpers.js:
```javascript
export function calculatePerHousehold(staffingCost, digitalCost, physicalCost, households) {
  if (households === 0) return null;
  return (staffingCost + digitalCost + physicalCost) / households;
}
```

From test/calculator.test.js:
```javascript
import { describe, it, expect } from 'vitest';
import { calculatePerHousehold } from '../src/js/lib/calculator-helpers.js';
```

From src/index.html (existing config.draft conditional pattern):
```nunjucks
{% if config.draft %}
<div ...>SAMPLE DATA</div>
{% endif %}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create analytics.js wrapper module and unit tests</name>
  <files>src/js/lib/analytics.js, test/analytics.test.js</files>
  <read_first>
    - src/js/lib/calculator-helpers.js (reference for lib helper pattern — JSDoc, export style)
    - test/calculator.test.js (reference for test pattern — vitest imports, describe/it structure)
  </read_first>
  <behavior>
    - fireEvent('staffing_selected', {level_id: '44hr-pt', label: 'Standard'}) calls window.gtag('event', 'staffing_selected', {level_id: '44hr-pt', label: 'Standard'}) when window.gtag is a function
    - fireEvent('staffing_selected', {...}) does NOT throw when window.gtag is undefined
    - trackStaffingSelected('44hr-pt', 'Standard') calls gtag with event name 'staffing_selected' and params {level_id: '44hr-pt', label: 'Standard'}
    - trackDigitalSliderChanged(55000) calls gtag with event name 'digital_slider_changed' and params {value_dollars: 55000}
    - trackPhysicalSliderChanged(10000) calls gtag with event name 'physical_slider_changed' and params {value_dollars: 10000}
    - trackCityToggled('providence', 'Providence', true) calls gtag with event name 'city_toggled' and params {city_id: 'providence', city_label: 'Providence', checked: true}
    - trackBreakdownOpened() calls gtag with event name 'breakdown_opened' and params {}
    - trackSharedUrlLoaded() calls gtag with event name 'shared_url_loaded' and params {params_detected: true}
    - All track* functions silently no-op when window.gtag is undefined (no throw)
  </behavior>
  <action>
    **RED phase:** Create `test/analytics.test.js` with vitest. Pattern: `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`. Import all 6 exported functions from `../src/js/lib/analytics.js`. Use `vi.fn()` to mock `window.gtag` in beforeEach, and delete it in afterEach. Write tests per the behavior block above. Run `pnpm test` — tests must FAIL (module does not exist yet).

    **GREEN phase:** Create `src/js/lib/analytics.js` following the calculator-helpers.js pattern (JSDoc, named exports). Structure:
    - Internal `fireEvent(eventName, params)` function: guards with `if (typeof window.gtag === 'function')` then calls `window.gtag('event', eventName, params || {})`
    - 6 named export functions, each calling fireEvent with the correct event name and parameter mapping:
      - `export function trackStaffingSelected(levelId, label)` -> fireEvent('staffing_selected', { level_id: levelId, label: label })
      - `export function trackDigitalSliderChanged(valueDollars)` -> fireEvent('digital_slider_changed', { value_dollars: valueDollars })
      - `export function trackPhysicalSliderChanged(valueDollars)` -> fireEvent('physical_slider_changed', { value_dollars: valueDollars })
      - `export function trackCityToggled(cityId, cityLabel, checked)` -> fireEvent('city_toggled', { city_id: cityId, city_label: cityLabel, checked: checked })
      - `export function trackBreakdownOpened()` -> fireEvent('breakdown_opened', {})
      - `export function trackSharedUrlLoaded()` -> fireEvent('shared_url_loaded', { params_detected: true })

    Run `pnpm test` — all tests must PASS.
  </action>
  <verify>
    <automated>pnpm test -- --reporter verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/js/lib/analytics.js` contains `export function trackStaffingSelected(`
    - `src/js/lib/analytics.js` contains `export function trackDigitalSliderChanged(`
    - `src/js/lib/analytics.js` contains `export function trackPhysicalSliderChanged(`
    - `src/js/lib/analytics.js` contains `export function trackCityToggled(`
    - `src/js/lib/analytics.js` contains `export function trackBreakdownOpened(`
    - `src/js/lib/analytics.js` contains `export function trackSharedUrlLoaded(`
    - `src/js/lib/analytics.js` contains `typeof window.gtag === 'function'`
    - `test/analytics.test.js` contains `describe(` and `import { trackStaffingSelected`
    - `pnpm test` exits 0
  </acceptance_criteria>
  <done>All 6 analytics wrapper functions exported and tested; guard logic verified for both gtag-present and gtag-absent cases.</done>
</task>

<task type="auto">
  <name>Task 2: Add ga4MeasurementId to config.js and GA4 snippet to index.html</name>
  <files>src/_data/config.js, src/index.html</files>
  <read_first>
    - src/_data/config.js (current state — see where to add field and NON-DEVELOPER EDIT GUIDE)
    - src/index.html (current state — see head section for snippet placement, existing config.draft pattern)
  </read_first>
  <action>
    **config.js changes:**
    1. Add to the NON-DEVELOPER EDIT GUIDE comment block (after the "IMPORTANT — compact URLs" section, before the closing `============` line), a new entry:

    ```
     *   - To enable analytics:    Change "ga4MeasurementId: null" to
     *                             your Google Analytics 4 Measurement ID.
     *                             It looks like: "G-XXXXXXXXXX".
     *                             Get it from analytics.google.com:
     *                             Admin > Data Streams > your stream >
     *                             Measurement ID. Keep the quotes.
     *                             Set back to null to disable.
    ```

    2. Add `ga4MeasurementId: null,` field to the exported config object. Place it after `draft: false,` (line 102) and before `footerLinks:` (line 104). Add a comment: `// Google Analytics 4 Measurement ID (see guide above)`

    **index.html changes:**
    Insert the following block in `<head>`, after the `<link rel="stylesheet" ...>` tag (line 8) and before `</head>` (line 9). Use the exact Nunjucks conditional and GA4 snippet:

    ```html
    {% if config.ga4MeasurementId %}
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id={{ config.ga4MeasurementId }}"></script>
    <script>
      if (location.hostname !== 'localhost' && location.hostname !== '127.0.0.1') {
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', '{{ config.ga4MeasurementId }}', {
          'allow_google_signals': false,
          'allow_ad_personalization_signals': false,
          'storage': 'none'
        });
      }
    </script>
    {% endif %}
    ```

    Per D-01, D-02, D-03, D-04, D-05, D-06: GA4 only, privacy-minimized (storage: none, no Google Signals, no ad personalization), config-driven on/off, hostname guard for localhost.
  </action>
  <verify>
    <automated>grep -c 'ga4MeasurementId' src/_data/config.js && grep -c 'ga4MeasurementId' src/index.html && grep -c "storage.*none" src/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `src/_data/config.js` contains `ga4MeasurementId: null,`
    - `src/_data/config.js` contains `To enable analytics:`
    - `src/_data/config.js` contains `analytics.google.com`
    - `src/index.html` contains `{% if config.ga4MeasurementId %}`
    - `src/index.html` contains `googletagmanager.com/gtag/js?id={{ config.ga4MeasurementId }}`
    - `src/index.html` contains `allow_google_signals`
    - `src/index.html` contains `allow_ad_personalization_signals`
    - `src/index.html` contains `'storage': 'none'`
    - `src/index.html` contains `location.hostname !== 'localhost'`
    - `src/index.html` contains `location.hostname !== '127.0.0.1'`
    - `src/index.html` contains `{% endif %}` (closing the GA4 conditional)
    - `pnpm test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>config.js has ga4MeasurementId field with edit guide; index.html has conditional GA4 snippet with privacy config and localhost guard.</done>
</task>

</tasks>

<verification>
- `pnpm test` passes (all existing + new analytics tests green)
- `grep 'ga4MeasurementId' src/_data/config.js` returns the null field
- `grep 'ga4MeasurementId' src/index.html` returns the conditional block
- `src/js/lib/analytics.js` exports all 6 tracking functions
</verification>

<success_criteria>
- analytics.js module exists with 6 named exports + gtag guard
- Unit tests cover guard logic (no-op when gtag absent) and event parameter shapes
- config.js has ga4MeasurementId: null with non-developer edit instructions
- index.html has conditional GA4 snippet with privacy config (storage: none) and localhost guard
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-usage-and-other-analytics-tracking/22-01-SUMMARY.md`
</output>
