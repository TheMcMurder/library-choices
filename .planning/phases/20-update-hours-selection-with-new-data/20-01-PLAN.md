---
phase: 20-update-hours-selection-with-new-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_data/config.js
  - test/url.test.js
autonomous: true
requirements:
  - HRS-01
must_haves:
  truths:
    - "staffingLevels has 3 tiers with real cost data from PDF (not PLACEHOLDER)"
    - "Tier 2 is marked as current service level"
    - "Schedule hours match CONTEXT decisions (35hr and 44hr tiers)"
    - "All existing tests pass without modification to test logic"
  artifacts:
    - path: "src/_data/config.js"
      provides: "Updated staffingLevels with real PDF data"
      contains: "annualCost: 56160"
    - path: "src/_data/config.js"
      provides: "New staffing IDs"
      contains: "35hr-pt"
  key_links:
    - from: "src/_data/config.js"
      to: "test/config.test.js"
      via: "import config"
      pattern: "staffingLevels.*id.*label.*annualCost"
    - from: "src/_data/config.js"
      to: "test/url.test.js"
      via: "config.staffingLevels[1].id"
      pattern: "staffingLevels\\[1\\]\\.id"
---

<objective>
Replace the three placeholder staffingLevels entries in config.js with real wage data derived from the Providence Library budget levels PDF. Update IDs, labels, costs, schedules, and descriptions per locked decisions D-01 through D-07.

Purpose: Move from placeholder staffing data to real librarian-provided budget figures so the calculator reflects actual costs citizens would vote on.
Output: Updated config.js with real staffing data; all tests green.
</objective>

<execution_context>
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-update-hours-selection-with-new-data/20-CONTEXT.md
@src/_data/config.js
@test/config.test.js
@test/url.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update staffingLevels data in config.js</name>
  <files>src/_data/config.js</files>
  <read_first>
    - src/_data/config.js (current schema, comment conventions, full file structure)
    - .planning/phases/20-update-hours-selection-with-new-data/20-CONTEXT.md (locked decisions D-01 through D-07 with exact values)
  </read_first>
  <action>
Replace the three objects in the `staffingLevels` array (lines 116-151 of config.js) with the following three objects. Preserve the array structure, comment style, and surrounding code exactly.

**Tier 1 (index 0):**
```js
{
  id: "35hr-pt",  // URL: compact uses array index; verbose id changed from v1
  label: "Essential",
  annualCost: 56160,
  schedule: [
    { days: "1-5", open: "11am", close: "5pm" },
    { days: "6",   open: "10am", close: "3pm" },
  ],
  description:
    "Part-time director and two part-time clerks, 35 hours per week. Two staff working at a time for safety. Some Saturday hours but no evening access.",
  source: "Providence Library budget levels (2026)",
},
```

**Tier 2 (index 1) — current service level:**
```js
{
  id: "44hr-pt",  // URL: compact uses array index; verbose id changed from v1
  label: "Standard",
  annualCost: 76440,
  schedule: [
    { days: "1-5", open: "10am", close: "6pm" },
    { days: "6",   open: "10am", close: "2pm" },
  ],
  description:
    "Part-time director and three part-time clerks, 44 hours per week. Two staff working at a time. Evening and Saturday hours for working citizens — the current operation level.",
  source: "Providence Library budget levels (2026)",
  isCurrentServiceLevel: true,
},
```

**Tier 3 (index 2):**
```js
{
  id: "44hr-fte",  // URL: compact uses array index; verbose id changed from v1
  label: "Full Service",
  annualCost: 160000,
  schedule: [
    { days: "1-5", open: "10am", close: "6pm" },
    { days: "6",   open: "10am", close: "2pm" },
  ],
  description:
    "Full-time director plus three part-time staff, 44 hours per week. Two or three staff at a time. The director would have the education and time to implement more programs, improve library practices, and perform outreach.",
  source: "Providence Library budget levels (2026)",
},
```

Per D-01: 3 tiers mapping PDF options 1-3, 4-5, and 6.
Per D-02: annualCost = staff wages only (56160, 76440, 160000).
Per D-03: Labels "Essential", "Standard", "Full Service".
Per D-04: Schedules — 35hr/wk and 44hr/wk as specified.
Per D-05: isCurrentServiceLevel on Tier 2.
Per D-06: New IDs "35hr-pt", "44hr-pt", "44hr-fte" with updated URL comment.
Per D-07: Descriptions drafted from PDF language, preserving two-staff safety note.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && npx vitest run test/config.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - config.js contains `id: "35hr-pt"` on the first staffing level
    - config.js contains `id: "44hr-pt"` on the second staffing level
    - config.js contains `id: "44hr-fte"` on the third staffing level
    - config.js contains `annualCost: 56160` (Tier 1)
    - config.js contains `annualCost: 76440` (Tier 2)
    - config.js contains `annualCost: 160000` (Tier 3)
    - config.js contains `label: "Essential"` (Tier 1)
    - config.js contains `label: "Standard"` (Tier 2)
    - config.js contains `label: "Full Service"` (Tier 3)
    - config.js contains `isCurrentServiceLevel: true` exactly once in staffingLevels, on the "44hr-pt" tier
    - config.js contains `open: "11am"` in Tier 1 schedule (Mon-Fri)
    - config.js contains `open: "10am"` in Tier 2 and Tier 3 schedules
    - config.js does NOT contain `annualCost: 150000` (old placeholder)
    - config.js does NOT contain `id: "1fte"` (old id)
    - `npx vitest run test/config.test.js` exits 0
  </acceptance_criteria>
  <done>staffingLevels array contains 3 tiers with real data from PDF; no PLACEHOLDER costs remain; config structure tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update URL test comment and verify all tests pass</name>
  <files>test/url.test.js</files>
  <read_first>
    - test/url.test.js (line 7 has hardcoded comment referencing old id "1fte-1pte")
    - src/_data/config.js (to confirm new id values after Task 1)
  </read_first>
  <action>
In test/url.test.js line 7, update the comment from `// "1fte-1pte"` to `// "44hr-pt"` to reflect the new id value at staffingLevels[1].

The test code itself (`config.staffingLevels[1].id`) is dynamic and does not need changing — only the trailing comment is stale.

Then run the full test suite to confirm everything passes.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/url.test.js line 7 contains the comment `// "44hr-pt"` (not `// "1fte-1pte"`)
    - `npx vitest run` exits 0 with all test files passing (config.test.js, url.test.js, calculator.test.js)
  </acceptance_criteria>
  <done>All tests pass; stale comment updated; no references to old staffing IDs remain in test files</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all 3 test files pass
2. `grep -c "PLACEHOLDER" src/_data/config.js` returns 0 for staffingLevels section (note: other PLACEHOLDERs may exist elsewhere)
3. `grep "annualCost" src/_data/config.js` shows 56160, 76440, 160000
4. `grep "isCurrentServiceLevel" src/_data/config.js` shows it on the "Standard" / "44hr-pt" tier
</verification>

<success_criteria>
- config.js staffingLevels replaced with 3 real tiers from PDF data
- Annual costs are 56160, 76440, 160000 (wages only, per D-02)
- Labels are Essential, Standard, Full Service (per D-03)
- Schedules reflect 35hr/wk and 44hr/wk (per D-04)
- isCurrentServiceLevel on Tier 2 Standard (per D-05)
- New IDs 35hr-pt, 44hr-pt, 44hr-fte (per D-06)
- All tests pass (config, url, calculator)
</success_criteria>

<output>
After completion, create `.planning/phases/20-update-hours-selection-with-new-data/20-01-SUMMARY.md`
</output>
