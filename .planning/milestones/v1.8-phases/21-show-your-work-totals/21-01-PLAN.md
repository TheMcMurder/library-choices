---
phase: 21-show-your-work-totals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/js/calculator.js
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "Collapsed result bar shows per-household cost on line 1 and total cost on line 2"
    - "Popover formula shows all 3 cost components (Hours Open, Digital, Physical) even when $0"
    - "Popover formula uses accounting-style layout with right-aligned amounts and left-aligned labels"
    - "Horizontal rule separates the three addends from the Total row"
    - "Division equation line appears below Total in the popover"
    - "Empty state (no cities selected) still shows plain text message and clears popover"
  artifacts:
    - path: "src/js/calculator.js"
      provides: "Two-state display logic in updateResult()"
      contains: "resultAmount.innerHTML"
  key_links:
    - from: "src/js/calculator.js"
      to: "#result-amount"
      via: "innerHTML write with two inner spans"
      pattern: "resultAmount\\.innerHTML"
    - from: "src/js/calculator.js"
      to: "#breakdown-detail"
      via: "innerHTML write with table element"
      pattern: "breakdownDetail\\.innerHTML"
---

## Objective

Expand the result bar so citizens can see the individual cost components (Hours Open, Digital, Physical) that sum to the total, not just the final per-household figure.

Purpose: Transparency — citizens should understand where their money goes, not just the bottom line.
Output: Updated `updateResult()` function in `calculator.js` with two-line collapsed bar and accounting-style formula popover.

## Execution Context

- Workflow: `.claude/get-shit-done/workflows/execute-plan.md`
- Summary template: `.claude/get-shit-done/templates/summary.md`

## Context

Read these files:
- `.planning/PROJECT.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/21-show-your-work-totals/21-CONTEXT.md`
- `.planning/phases/21-show-your-work-totals/21-UI-SPEC.md`
- `.planning/phases/21-show-your-work-totals/21-RESEARCH.md`

### Interfaces

Key function and DOM element contracts the executor needs.

From src/js/calculator.js (the only file being modified):

```javascript
// These local variables are already computed inside updateResult():
var staffingCost = getStaffingCost();       // number — annual staffing cost
var digitalCost = getDigitalCost();         // number — annual digital collections cost
var physicalCost = getPhysicalCost();       // number — annual physical collections cost
var totalCost = staffingCost + digitalCost + physicalCost;  // number
var perHousehold = calculatePerHousehold(staffingCost, digitalCost, physicalCost, totalHouseholds);  // number

// DOM elements already queried:
var resultAmount = document.getElementById('result-amount');     // <span> in sticky bar
var breakdownDetail = document.getElementById('breakdown-detail'); // <div> popover container
```

From src/index.html (read-only context — DO NOT modify this file):
- `#result-bar` has `aria-live="polite" aria-atomic="true"` — screen readers announce changes
- `#breakdown-detail` has classes `absolute bottom-full mb-2 right-0 bg-blue-900 text-white text-sm px-4 py-2 rounded-lg shadow-lg w-64`
- `#breakdown-toggle` is the info icon button that toggles `breakdownDetail.hidden`

## Tasks

### Task 1: Update updateResult() with two-line bar and formula popover

- **Type:** auto
- **Files:** `src/js/calculator.js`

**read_first:**
- `src/js/calculator.js` (current state of updateResult — lines 37-58)
- `.planning/phases/21-show-your-work-totals/21-UI-SPEC.md` (exact HTML structure and Tailwind classes)
- `.planning/phases/21-show-your-work-totals/21-RESEARCH.md` (full code example in "Code Examples" section)

**action:**

Modify the `updateResult()` function in `src/js/calculator.js`. Three changes total, all inside this one function. The RESEARCH.md "Code Examples" section contains the complete updated function — use it as the reference implementation.

**Change 1 — Empty state (line 45): per D-02, Pitfall 3**

Change `breakdownDetail.textContent = '';` to `breakdownDetail.innerHTML = '';`

Keep `resultAmount.textContent` and `resultAmount.className = 'text-base text-blue-100'` as-is for the empty state.

**Change 2 — Collapsed bar (lines 55-56): per D-01**

Replace:

```javascript
resultAmount.textContent = '$' + perHousehold.toFixed(2) + '/household/year';
resultAmount.className = 'text-2xl font-semibold';
```

With two innerHTML spans — one `block text-2xl font-semibold` for the per-household line, one `block text-sm text-blue-200` for the total line. Clear `resultAmount.className = ''` so the outer element does not interfere with inner span classes.

The exact HTML string template is in 21-RESEARCH.md under "Collapsed bar pattern (from UI-SPEC)".

**Change 3 — Popover formula (line 57): per D-02, D-03, D-04, D-05, D-06**

Replace the single `breakdownDetail.textContent` line with an innerHTML write that renders an HTML table with:
- tbody: 3 rows (Hours Open, Digital, Physical) — each row has a `text-right pr-3 tabular-nums` td for the dollar amount and a `text-blue-200` td for the label
- tfoot: separator hr row (`border-blue-700`), Total row (`font-semibold`), division equation row (`text-blue-200`)

All 3 component rows are always rendered, even when $0 (per D-04 — no conditional hiding).

The exact HTML string template is in 21-RESEARCH.md under "Expanded popover pattern (from UI-SPEC)".

**What NOT to change:**
- Do NOT modify `src/index.html` — all content is JS-injected
- Do NOT add conditional hiding for $0 rows (D-04 requires all 3 always shown)
- Do NOT modify any function other than `updateResult()`
- Do NOT change the `calculatePerHousehold` import or any other import

**verify:** `pnpm test` exits 0

**acceptance_criteria:**
- calculator.js contains `resultAmount.innerHTML =` (switched from textContent)
- calculator.js contains `breakdownDetail.innerHTML =` (switched from textContent)
- calculator.js contains `resultAmount.className = ''` (clears outer classes on happy path)
- calculator.js contains `breakdownDetail.innerHTML = ''` in the empty-state block (not textContent)
- calculator.js contains the string `Hours Open` (D-06 label)
- calculator.js contains the string `Digital` as a table cell label (D-06 label)
- calculator.js contains the string `Physical` as a table cell label (D-06 label)
- calculator.js contains the string `Total` as a table cell label
- calculator.js contains `border-blue-700` (horizontal rule separator per D-05)
- calculator.js contains `tabular-nums` (right-aligned monospace amounts per D-05)
- calculator.js contains `text-blue-200` (subdued label color per UI-SPEC)
- calculator.js contains `text-2xl font-semibold` inside an innerHTML span (primary line)
- calculator.js contains `text-sm text-blue-200` inside an innerHTML span (secondary total line per D-01)
- calculator.js empty-state block still uses `resultAmount.textContent = 'Select at least one city'` (not innerHTML)
- calculator.js empty-state block still sets `resultAmount.className = 'text-base text-blue-100'`
- `pnpm test` exits 0 (all existing tests pass)

**done:** updateResult() writes two-line innerHTML for collapsed bar and accounting-style table innerHTML for popover. Empty state uses innerHTML = '' for breakdown. All existing tests pass.

### Task 2: Visual verification of result bar and popover

- **Type:** checkpoint:human-verify
- **Gate:** blocking
- **Files:** `src/js/calculator.js`

**action:**

Human verifies the visual output of Task 1. What was built: Two-line collapsed result bar showing per-household cost plus total, and accounting-style formula popover showing Hours Open / Digital / Physical cost breakdown with separator line, Total, and division equation.

Steps to verify:
1. Run `pnpm dev` (or open the built site in a browser)
2. Select at least one city checkbox — verify the sticky result bar shows:
   - Line 1 (large): `$X.XX/household/year`
   - Line 2 (smaller, blue-200): `$X,XXX total`
3. Click the info icon toggle button — verify the popover shows:
   - Three rows: Hours Open, Digital, Physical — each with right-aligned dollar amount
   - All three rows present even if one is $0
   - Horizontal rule separator between addends and Total
   - Total row in semibold
   - Division equation: `$X,XXX / N households = $X.XX/year`
4. Uncheck all cities — verify bar shows "Select at least one city" and popover content is cleared
5. On mobile viewport (375px width) — verify formula fits without horizontal overflow

Resume signal: Type "approved" or describe issues.

**verify:** Human visual confirmation

**done:** User approves visual layout of collapsed bar and expanded popover on desktop and mobile viewports.

## Verification

- `pnpm test` passes (all existing unit tests green)
- Visual inspection confirms two-line collapsed bar and accounting-style formula popover
- All 3 cost components visible in popover even when one is $0
- Empty state (no cities) still works correctly

## Success Criteria

- Result bar displays per-household cost AND total cost in collapsed state
- Popover shows Hours Open, Digital, Physical as separate line items with right-aligned amounts
- Horizontal rule separates addends from Total
- Division equation appears below Total
- Existing tests pass unchanged

## Output

After completion, create `.planning/phases/21-show-your-work-totals/21-01-SUMMARY.md`
