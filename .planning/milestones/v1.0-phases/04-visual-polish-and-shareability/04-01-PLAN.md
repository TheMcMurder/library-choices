---
phase: 04-visual-polish-and-shareability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.html
  - src/js/calculator.js
  - src/_data/config.js
autonomous: true
requirements:
  - DESG-01
  - DESG-02
  - DESG-03

must_haves:
  truths:
    - "Page has a blue header bar with site name and subtitle above the form"
    - "Sticky result bar is pinned to bottom of viewport showing the dollar amount"
    - "Breakdown tooltip expands when ? icon is clicked and dismisses on click-outside"
    - "Footer shows attribution and civic links section driven from config.js"
    - "DRAFT overlay renders as a diagonal semi-transparent stamp when config.draft is true"
    - "No horizontal scroll at 375px viewport width"
    - "Bottom form content is not obscured by the sticky bar (padding applied)"
  artifacts:
    - path: "src/index.html"
      provides: "Complete page template with header, footer, sticky bar, DRAFT overlay"
      contains: "id=\"result-bar\""
    - path: "src/js/calculator.js"
      provides: "Calculator targeting sticky result bar instead of #result"
      contains: "result-bar"
    - path: "src/_data/config.js"
      provides: "draft boolean and footerLinks array"
      contains: "draft:"
  key_links:
    - from: "src/js/calculator.js"
      to: "#result-bar"
      via: "getElementById or querySelector"
      pattern: "result-bar"
    - from: "src/index.html"
      to: "config.draft"
      via: "Nunjucks conditional"
      pattern: "if config.draft"
    - from: "src/index.html"
      to: "config.footerLinks"
      via: "Nunjucks for loop"
      pattern: "for link in config.footerLinks"
---

<objective>
Transform the page from a functional prototype into a civic-quality production layout with a blue header bar, sticky result bar replacing the old #result div, footer with civic links, DRAFT overlay, and mobile-first responsive corrections.

Purpose: Delivers DESG-01 (civic design), DESG-02 (mobile-first responsive), and DESG-03 (clean Tailwind design language).
Output: Updated index.html, calculator.js, and config.js.
</objective>

<execution_context>
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-polish-and-shareability/04-CONTEXT.md
@.planning/phases/04-visual-polish-and-shareability/04-RESEARCH.md
@.planning/phases/04-visual-polish-and-shareability/04-UI-SPEC.md

<interfaces>
<!-- Current index.html structure (being replaced): -->
<!-- <main class="max-w-2xl mx-auto px-8 py-12"> with <h1> inside, #result div at bottom -->
<!-- calculator.js targets: document.getElementById('result') -->
<!-- config.js exports: { siteName, staffingLevels[], collections{}, cities[] } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config.js data fields + restructure index.html template</name>
  <files>src/_data/config.js, src/index.html</files>
  <read_first>
    - src/_data/config.js (current structure — add draft + footerLinks fields)
    - src/index.html (current template — full restructure)
    - .planning/phases/04-visual-polish-and-shareability/04-UI-SPEC.md (component inventory + layout contract)
    - .planning/phases/04-visual-polish-and-shareability/04-CONTEXT.md (locked decisions)
  </read_first>
  <action>
**config.js changes:**

Add two new fields to the exported object, after `siteName`:

```javascript
draft: true, // Set to false when real numbers are finalized

footerLinks: [
  {
    label: "County-wide ballot initiative",
    url: "https://example.com/ballot" // Site owner will supply real URL
  },
  {
    label: "Friends of the Library",
    url: "https://example.com/friends" // Site owner will supply real URL
  }
],
```

Add a comment in the NON-DEVELOPER EDIT GUIDE block explaining:
```
 *   - To remove the DRAFT
 *     watermark:              Change "draft: true" to
 *                             "draft: false" (no quotes around
 *                             true/false).
```

**index.html restructure:**

Replace the entire template with the following structure (keeping all existing form fieldset content intact):

1. **Page header** (before `<main>`):
```html
<header class="bg-blue-800 text-white px-4 py-4 sm:px-8">
  <h1 class="text-xl font-semibold">{{ config.siteName }}</h1>
  <p class="text-sm text-blue-200 mt-1">Explore library funding options for our community</p>
</header>
```

2. **Main content** — change `<main>` classes from `max-w-2xl mx-auto px-8 py-12` to `max-w-2xl mx-auto px-4 sm:px-8 py-8 pb-16`. Remove the `<h1>` that was inside `<main>` (it moved to header). Remove the entire `<div id="result">` block (lines 88-91 of current template).

3. **Footer** (after `</main>`):
```html
<footer class="bg-gray-50 border-t border-gray-200 px-4 py-8 sm:px-8 mt-8 text-sm text-gray-600">
  <div class="max-w-2xl mx-auto">
    <p class="text-gray-500">Data: Cache County records. Built by [builder name].</p>
    {% if config.footerLinks and config.footerLinks.length %}
    <div class="mt-4">
      <p class="font-semibold text-gray-900 mb-2">Not happy with these choices?</p>
      <p class="mb-2">Explore other options for your community:</p>
      {% for link in config.footerLinks %}
        <a href="{{ link.url }}" class="text-blue-700 underline hover:text-blue-900 block mb-1" target="_blank" rel="noopener noreferrer">{{ link.label }}</a>
      {% endfor %}
    </div>
    {% endif %}
  </div>
</footer>
```

4. **DRAFT overlay** (after `</footer>`, before sticky bar):
```html
{% if config.draft %}
<div class="fixed inset-0 z-40 pointer-events-none flex items-center justify-center overflow-hidden" aria-hidden="true">
  <span class="text-[12vw] font-semibold text-red-600 opacity-15 rotate-[-30deg] select-none tracking-widest uppercase">DRAFT</span>
</div>
{% endif %}
```

5. **Sticky result bar** (after DRAFT overlay, before scripts):
```html
<div id="result-bar" class="fixed bottom-0 left-0 right-0 z-50 bg-blue-800 text-white flex items-center justify-between px-4 py-4 shadow-lg relative" aria-live="polite" aria-atomic="true">
  <span id="result-amount" class="text-2xl font-semibold">Select options above</span>
  <button id="breakdown-toggle" type="button" aria-label="Show cost breakdown" class="p-2 rounded hover:bg-blue-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-white focus-visible:outline-offset-2">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="size-5" aria-hidden="true" focusable="false">
      <path fill-rule="evenodd" d="M18 10a8 8 0 1 1-16 0 8 8 0 0 1 16 0ZM8.94 6.94a.75.75 0 1 1-1.061-1.061 3 3 0 1 1 2.871 5.026v.345a.75.75 0 0 1-1.5 0v-.5c0-.72.57-1.172 1.081-1.287A1.5 1.5 0 1 0 8.94 6.94ZM10 15a1 1 0 1 0 0-2 1 1 0 0 0 0 2Z" clip-rule="evenodd" />
    </svg>
  </button>
  <div id="breakdown-detail" hidden class="absolute bottom-full left-0 right-0 bg-blue-900 text-sm text-white px-4 py-2">
  </div>
</div>
```

6. **Script tags** — keep existing `window.LIBRARY_DATA` and `calculator.js` script tags unchanged. (url.js will be added in Plan 02.)

**Document order summary:** `<header>` -> `<main>` (form only) -> `<footer>` -> DRAFT overlay -> `#result-bar` -> scripts.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/_data/config.js contains `draft: true`
    - src/_data/config.js contains `footerLinks:`
    - src/_data/config.js contains `"To remove the DRAFT"`
    - src/index.html contains `<header class="bg-blue-800`
    - src/index.html contains `id="result-bar"`
    - src/index.html contains `id="result-amount"`
    - src/index.html contains `id="breakdown-toggle"`
    - src/index.html contains `id="breakdown-detail"`
    - src/index.html contains `config.draft`
    - src/index.html contains `config.footerLinks`
    - src/index.html contains `<footer`
    - src/index.html contains `px-4 sm:px-8` (mobile-first padding on main)
    - src/index.html contains `pb-16` (bottom padding for sticky bar clearance)
    - src/index.html does NOT contain `id="result"` (old result div removed)
    - src/index.html does NOT contain `px-8 py-12` (old main padding removed)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>
    index.html has full page structure: header bar, mobile-safe main, footer with civic links, DRAFT overlay conditional, sticky result bar with breakdown tooltip. config.js has draft boolean and footerLinks array. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retarget calculator.js to sticky result bar with breakdown toggle</name>
  <files>src/js/calculator.js</files>
  <read_first>
    - src/js/calculator.js (current calculator targeting #result)
    - src/index.html (after Task 1 — to see new #result-bar, #result-amount, #breakdown-detail, #breakdown-toggle IDs)
    - .planning/phases/04-visual-polish-and-shareability/04-UI-SPEC.md (interaction contract section)
  </read_first>
  <action>
Rewrite `calculator.js` to target the sticky result bar elements instead of `#result`. Keep the IIFE + `'use strict'` pattern. Keep all existing cost calculation logic (`getStaffingCost`, `getCollectionsCost`, `getTotalHouseholds`).

**Changes to `updateResult()`:**

Replace the `resultEl.innerHTML` assignments with targeted element updates:

```javascript
const resultAmount = document.getElementById('result-amount');
const breakdownDetail = document.getElementById('breakdown-detail');
```

When `totalHouseholds === 0`:
- Set `resultAmount.textContent = 'Select at least one city'`
- Set `resultAmount.className = 'text-base text-blue-100'` (body size, lighter tone per UI-SPEC)
- Set `breakdownDetail.textContent = ''`

When calculating normally:
- Set `resultAmount.textContent = '$' + perHousehold.toFixed(2) + '/household/year'`
- Set `resultAmount.className = 'text-2xl font-semibold'` (restore display style)
- Set `breakdownDetail.textContent = '$' + totalCost.toLocaleString('en-US') + ' total \u00f7 ' + totalHouseholds.toLocaleString('en-US') + ' households'`

**Add breakdown toggle + click-outside logic:**

After the `updateResult` function definition, add:

```javascript
var toggleBtn = document.getElementById('breakdown-toggle');
var breakdownDetail = document.getElementById('breakdown-detail');

toggleBtn.addEventListener('click', function (e) {
  e.stopPropagation();
  var isHidden = breakdownDetail.hidden;
  breakdownDetail.hidden = !isHidden;
  toggleBtn.setAttribute('aria-label', isHidden ? 'Hide cost breakdown' : 'Show cost breakdown');
});

document.addEventListener('click', function (e) {
  if (!breakdownDetail.contains(e.target) && e.target !== toggleBtn && !toggleBtn.contains(e.target)) {
    breakdownDetail.hidden = true;
    toggleBtn.setAttribute('aria-label', 'Show cost breakdown');
  }
});
```

**Remove:** The old `const resultEl = document.getElementById('result');` line (element no longer exists).

**Keep:** The `form.addEventListener('change', updateResult);` delegated listener and the `updateResult();` call at the bottom of the IIFE.
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/js/calculator.js contains `getElementById('result-amount')`
    - src/js/calculator.js contains `getElementById('breakdown-detail')`
    - src/js/calculator.js contains `getElementById('breakdown-toggle')`
    - src/js/calculator.js contains `'Select at least one city'`
    - src/js/calculator.js contains `/household/year`
    - src/js/calculator.js contains `aria-label`
    - src/js/calculator.js contains `breakdownDetail.hidden`
    - src/js/calculator.js contains `stopPropagation`
    - src/js/calculator.js does NOT contain `getElementById('result')` (old target removed)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>
    calculator.js updates the sticky bar's #result-amount with the dollar figure (or zero-city message), populates #breakdown-detail with the breakdown line, and handles the toggle button with click-outside dismissal. The old #result targeting is completely removed. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` exits 0 — Eleventy renders template with all new Nunjucks conditionals
2. Built HTML contains `id="result-bar"` and does NOT contain `id="result"`
3. Built HTML contains `<header class="bg-blue-800` (header bar rendered)
4. Built HTML contains `DRAFT` (overlay rendered because config.draft is true)
5. Built HTML contains footer links section
6. calculator.js references `result-amount`, `breakdown-detail`, `breakdown-toggle`
</verification>

<success_criteria>
- Page has civic header bar (blue-800 bg, white text, site name + subtitle)
- Sticky result bar is fixed at bottom with dollar amount display
- Breakdown tooltip toggles on ? icon click, dismisses on click-outside
- Footer with attribution and civic links (data-driven from config.footerLinks)
- DRAFT overlay renders as diagonal semi-transparent stamp (controlled by config.draft)
- Main content uses px-4 sm:px-8 (no horizontal scroll at 375px)
- Main content has pb-16 (bottom padding so sticky bar does not obscure form)
- Old #result div is completely removed
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish-and-shareability/04-01-SUMMARY.md`
</output>
