---
phase: 01-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - INFR-02

must_haves:
  truths:
    - "GitHub Actions workflow triggers on push to main"
    - "Workflow runs npm ci and npm run build"
    - "Workflow uploads _site/ as a Pages artifact and deploys it"
    - "Deployed page loads at the GitHub Pages URL with no 404 errors on any asset"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions workflow for building and deploying to Pages"
      contains: "actions/deploy-pages"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "package.json"
      via: "npm ci installs deps, npm run build produces _site/"
      pattern: "npm run build"
    - from: ".github/workflows/deploy.yml"
      to: "_site/"
      via: "upload-pages-artifact uploads the build output"
      pattern: "path: _site"
---

<objective>
Create the GitHub Actions workflow that builds and deploys the site to GitHub Pages on every push to main.

Purpose: Complete the CI/CD pipeline so the site is automatically deployed and accessible at the public GitHub Pages URL. This is the final piece of Phase 1 infrastructure.
Output: A working deploy.yml workflow file and a verified live deployment.
</objective>

<execution_context>
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mcmurdie/Development/personal/library-choices/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@.planning/phases/01-scaffolding/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    package.json
    .planning/phases/01-scaffolding/01-RESEARCH.md
  </read_first>
  <action>
1. Create directories `.github/workflows/`.

2. Create `.github/workflows/deploy.yml` with this exact content:
   ```yaml
   name: Deploy to GitHub Pages

   on:
     push:
       branches: [main]

   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - name: Setup Node
           uses: actions/setup-node@v4
           with:
             node-version: "22"
         - name: Cache npm
           uses: actions/cache@v4
           with:
             path: ~/.npm
             key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
         - run: npm ci
         - run: npm run build
         - name: Upload artifact
           uses: actions/upload-pages-artifact@v3
           with:
             path: _site/

     deploy:
       needs: build
       runs-on: ubuntu-latest
       environment:
         name: github-pages
         url: ${{ steps.deployment.outputs.page_url }}
       permissions:
         pages: write
         id-token: write
       steps:
         - name: Deploy to GitHub Pages
           id: deployment
           uses: actions/deploy-pages@v4
   ```

   Key details:
   - Triggers on push to `main` branch only
   - Uses Node.js 22 (current LTS)
   - Caches npm dependencies using package-lock.json hash
   - `npm ci` for reproducible installs (not `npm install`)
   - `npm run build` runs Tailwind then Eleventy sequentially
   - `upload-pages-artifact@v3` uploads `_site/` directory
   - `deploy-pages@v4` deploys to GitHub Pages
   - `permissions` block with `pages: write` and `id-token: write` is required (Pitfall 4 from research)
   - The `deploy` job `needs: build` so it only runs after successful build

3. Verify the YAML is valid by checking it parses:
   ```
   python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml')); print('YAML VALID')"
   ```
  </action>
  <verify>
    <automated>cd /Users/mcmurdie/Development/personal/library-choices && test -f .github/workflows/deploy.yml && grep -q "actions/deploy-pages@v4" .github/workflows/deploy.yml && grep -q "npm run build" .github/workflows/deploy.yml && grep -q "pages: write" .github/workflows/deploy.yml && grep -q "id-token: write" .github/workflows/deploy.yml && echo "WORKFLOW FILE VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml exists
    - deploy.yml contains `on:` with `push:` and `branches: [main]`
    - deploy.yml contains `actions/checkout@v4`
    - deploy.yml contains `actions/setup-node@v4` with `node-version: "22"`
    - deploy.yml contains `actions/cache@v4` with `path: ~/.npm`
    - deploy.yml contains `npm ci`
    - deploy.yml contains `npm run build`
    - deploy.yml contains `actions/upload-pages-artifact@v3` with `path: _site/`
    - deploy.yml contains `actions/deploy-pages@v4`
    - deploy.yml contains `permissions:` with `pages: write` and `id-token: write`
    - deploy.yml contains `needs: build` in the deploy job
  </acceptance_criteria>
  <done>GitHub Actions workflow file exists with correct build, upload, and deploy steps including required permissions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GitHub Pages deployment</name>
  <files>none</files>
  <action>
This is a human verification checkpoint. The executor should push the committed workflow to the remote repository (if not already pushed), then present the verification steps below to the user.

Before presenting to user, run: `gh workflow list` to confirm the workflow file is recognized by GitHub.
  </action>
  <verify>
    <automated>gh workflow list 2>/dev/null | grep -q "Deploy to GitHub Pages" && echo "WORKFLOW REGISTERED" || echo "Push to remote first"</automated>
  </verify>
  <what-built>
    Complete build and deploy pipeline: Eleventy v3 + Tailwind CSS v4 project with GitHub Actions workflow that builds and deploys to GitHub Pages on push to main.
  </what-built>
  <how-to-verify>
    **Pre-requisite (one-time repo setup):**
    1. Go to your repository Settings on GitHub
    2. Navigate to Pages (left sidebar)
    3. Under "Build and deployment" > Source, select "GitHub Actions" (not "Deploy from a branch")

    **Trigger deployment:**
    1. The previous task's commit + push to main should trigger the workflow automatically
    2. Go to the repository's Actions tab on GitHub
    3. Wait for the "Deploy to GitHub Pages" workflow to complete (green checkmark)

    **Verify the deployed site:**
    4. Visit `https://mcmurdie.github.io/library-choices/` (or the URL shown in the Actions deploy step output)
    5. Confirm the page loads with the heading "Cache County Library Choices"
    6. Confirm the text is styled (not plain unstyled HTML — you should see the Tailwind utilities applied: bold heading, gray body text, padded layout)
    7. Open browser DevTools > Network tab, reload the page, confirm no 404 errors on any asset (especially `compiled.css`)

    **If the workflow fails:**
    - Check that Pages source is set to "GitHub Actions" (not "Deploy from a branch")
    - Check the Actions log for the specific error message
  </how-to-verify>
  <acceptance_criteria>
    - GitHub Actions workflow "Deploy to GitHub Pages" appears in `gh workflow list`
    - User confirms the deployed page loads at the GitHub Pages URL
    - User confirms no 404 errors on assets (CSS loads correctly)
    - User confirms text is styled with Tailwind utilities
  </acceptance_criteria>
  <done>User has confirmed the deployed page loads at the GitHub Pages URL with styled content and no 404 errors on any asset.</done>
  <resume-signal>Type "approved" if the deployed page loads correctly with styled content and no 404s, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
1. `.github/workflows/deploy.yml` exists with correct content
2. GitHub Actions workflow runs successfully on push to main
3. Deployed page loads at GitHub Pages URL
4. No 404 errors on any asset (CSS, JS)
5. Page shows styled content (Tailwind CSS is working)
</verification>

<success_criteria>
- GitHub Actions workflow file is committed and triggers on push to main
- The workflow builds the site and deploys to GitHub Pages without errors
- The deployed page at the GitHub Pages URL shows styled content with no broken asset references
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-02-SUMMARY.md`
</output>
